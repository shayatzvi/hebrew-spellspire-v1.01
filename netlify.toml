[build]
  publish = "."  # Adjust this to your publish directory

# Ensure Netlify Forms are properly processed
[build.processing]
  skip_processing = false

[build.processing.html]
  pretty_urls = true 